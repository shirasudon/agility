// @format

import { onMessage, onOpen, onClose, onError } from './websocket'

describe('onMessage', () => {
  it('')
})

describe('onOpen', () => {
  it('')
})

describe('onClose', () => {
  it('')
})
describe('onError', () => {
  it('')
})
